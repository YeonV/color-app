/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // async rewrites() {
  //   return [
  //     {
  //       source: '/api/socket.io',
  //       destination: '/socket.io', // Map the API route to the socket.io endpoint
  //     },
  //   ]
  // },
}

module.exports = nextConfig
